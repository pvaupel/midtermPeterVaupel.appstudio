/*
btnSubmit.onclick=function(){
  let userIncome = Number(inptIncome.value)
  
    if (userIncome < 30000) {
    lblBracket.textContent = `With your income of $${userIncome}, you are in the tax bracket of 8%. `
  } else if ( userIncome < 99999 && userIncome >= 30000) {
    lblBracket.textContent = `With your income of $${userIncome}, you are in the tax bracket of 15%. `
  } else if ( userIncome >= 99999) {
    lblBracket.textContent = `With your income of $${userIncome}, you are in the tax bracket of 25%. `
    }
}

btnAgain.onclick=function(){
  lblBracket.textContent = `Enter an income!`
}
*/